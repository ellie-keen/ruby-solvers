# Create a method which takes an array of integers as an argument.
# Returns a new array, with the values doubled.

def double_numbers(array)
	array.map { |integer| integer * 2 }	
end

# This method which takes two arrays as arguments,
# should return a new array as follows

# a = [1, 2, 3]
# b = [4, 5, 6]

# combine_arrays(a, b)
# returns --> [1, 4, 2, 5, 3, 6]

def combine_arrays(array_1, array_2)
end
